enclave {
    from "sgx_tstdc.edl" import *;

    trusted {
        /* define ECALLs here. */
        public void accessPD([out, size=len] char* outbuf, size_t len, int i);
    };

    untrusted {
        /* define OCALLs here. */

    };
};
